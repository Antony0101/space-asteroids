{
    "name": "space-asteroids",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --host",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "test": "vitest run",
        "test-dev": "vitest --watch"
    },
    "devDependencies": {
        "prettier": "^3.4.1",
        "typescript": "~5.6.2",
        "vite": "^6.0.1",
        "vitest": "^2.1.8"
    }
}
